```javascript
// 复制password到粘贴板
const password   //需要被拷贝的字符
const input = document.createElement('input')
input.value = password
document.body.appendChild(input)
input.select()
document.execCommand('copy')
document.body.removeChild(input)
```