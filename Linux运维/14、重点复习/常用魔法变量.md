```
{{ inventory_hostname }}                          #主机清单                 
{{ ansible_memtotal_mb  | string }}               #以mb表示总内存大小,并标注为string类型
{{ ansible_bios_version  }}                       #BIOS版本
{{ ansible_devices.vdb.size | default('NONE') }}  #设备vdb大小，如果不存在默认为NONE
{{ ansible_fqdn }}                    #完全限定域名
{{ ansible_default_ipv4.address }}    #默认ip地址
{{ group_names}} #所有的分组名称，是一个列表
{{ groups.all }} #所有分组里面的所有的主机
```